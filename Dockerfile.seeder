FROM python:3.12-slim

WORKDIR /app

# pyproject.toml 복사 및 빌드 의존성 최소 설치
COPY pyproject.toml /app/
RUN pip install --no-cache-dir .

# 소스코드 및 구동 스크립트 복사
COPY src/ /app/src/
COPY scripts/ /app/scripts/

# 실행 명령
CMD ["python", "scripts/populate_graph.py"]
